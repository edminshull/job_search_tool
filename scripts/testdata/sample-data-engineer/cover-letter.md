# Cover letter — Data Engineer, Sample Co

**To:** Hiring Team, Sample Co
**Role:** Data Engineer (London)

> **Editor's note (not sent).** Only the `**Key:** value` lines above are used as
> metadata; this note is ignored. Everything below the `---` is the letter body,
> one paragraph per blank-line-separated block. Name and contact are pulled from
> `cv/master.yaml` so they never drift from the CV.

---

Dear Hiring Team,

This is a fictional fixture letter used only by the self-tests. It exists because
the separator above is described in the note using the literal characters that
also form the separator.

Second paragraph, so the parser has more than one block to split on.
